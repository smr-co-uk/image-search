
from PIL import Image

class ImageSearch:
    def find_similar_image(self, query_image: Image, threshold=1) -> list[str]:
        pass

    def index_images(self, algorithm: str = "average_hash", verbose: bool = False) -> None:
        pass
